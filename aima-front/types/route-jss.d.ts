// Temporary module declarations to satisfy generated Next.js types
// Some Next.js generated files reference `route.jss` due to a generator quirk.
// Provide a fallback declaration so TypeScript doesn't emit TS2307 in editor.

declare module "*/route.jss" {
  const mod: any;
  export = mod;
}

declare module "../../../app/nextapi/route.jss" {
  const mod: any;
  export = mod;
}
